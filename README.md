# Matlab Custom Package Plugin README

## Features

1. Auto-completion of custom package, including `current directory`, `paths in addpath command`.
2. Jump-to-definition of current file, including `body`, `functin line`

**Enjoy!**